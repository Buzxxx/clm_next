import React from "react";
import VendorPage from "@/components/vendor/features/vendorPage";

const VendorLayout = ({ id }: { id: string }) => {
  return <VendorPage id={id} />;
};

export default VendorLayout;
