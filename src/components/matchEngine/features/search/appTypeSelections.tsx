import React from "react";
import AppFeature from "../app/appFeatures/appFeature";

const AppTypeSelection = () => {
  return (
    <div className="bg-background w-full min-h-screen relative">
      <AppFeature ref="search" />
    </div>
  );
};

export default AppTypeSelection;
